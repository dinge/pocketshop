div :class => :harras do
  dl do
    dt "hallo"
    dd "echo"
    dt "alter"
    dd "pisser"

    @items.each do |item|
      dt "name"
      dd item.name
      # dt "render partial definition"
      # dd render(:partial => 'here')
      dt "text from helper"
      dd sux_helper_text(item)
    end
  end
end

span do
  **** "suppe"
end


